# 🚀 TrueSpace - Quick Start Guide

Быстрое руководство для запуска проекта за 5 минут.

## Шаг 1: Клонирование (30 секунд)

```bash
git clone https://github.com/yourusername/truespace.git
cd truespace
```

## Шаг 2: Установка зависимостей (2 минуты)

```bash
npm install
```

## Шаг 3: Настройка переменных окружения (1 минута)

Создайте файл `.env`:

```bash
cp .env.local.example .env
```

Минимальная конфигурация для локальной разработки:

```env
# MongoDB (используйте свою строку подключения)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/truespace

# JWT Secrets (сгенерируйте любые случайные строки)
JWT_SECRET=your-random-secret-key-min-32-chars
NEXTAUTH_SECRET=another-random-secret-key-min-32-chars
NEXTAUTH_URL=http://localhost:3000

# AWS S3 (опционально для начала)
AWS_ACCESS_KEY_ID=your-key
AWS_SECRET_ACCESS_KEY=your-secret
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket

# Email (опционально для начала)
EMAIL_SERVER=smtp://user:pass@smtp.gmail.com:587
EMAIL_FROM=noreply@truespace.com
```

### Быстрая генерация секретов:

```bash
# Сгенерировать JWT_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Сгенерировать NEXTAUTH_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## Шаг 4: Запуск (30 секунд)

```bash
npm run dev
```

Откройте браузер: **http://localhost:3000** 🎉

## 🎓 Первые шаги

### 1. Зарегистрируйтесь

1. Перейдите на http://localhost:3000/register
2. Создайте аккаунт
3. Вы будете перенаправлены на страницу промокода

### 2. Станьте администратором

Откройте MongoDB Compass или Atlas и найдите вашего пользователя:

```javascript
// В коллекции users измените:
{
  "role": "admin",
  "hasAccessToCourses": true
}
```

Или через MongoDB Shell:

```javascript
db.users.updateOne(
  { email: "your@email.com" },
  { 
    $set: { 
      role: "admin",
      hasAccessToCourses: true 
    }
  }
)
```

### 3. Создайте промокод

1. Войдите как администратор
2. Перейдите на `/admin`
3. Выберите "Promo Codes"
4. Создайте код, например: `WELCOME2024`

### 4. Создайте тестовый курс

1. Перейдите на `/admin`
2. Выберите "Courses"
3. Нажмите "Create New Course"
4. Заполните данные:
   - **Title**: "Introduction to Web Development"
   - **Description**: "Learn the basics of web development"
   - **Category**: "Programming"
   - **Level**: "Beginner"
   - **Instructor**: "John Doe"
   - **Thumbnail URL**: (используйте любое изображение)

## 📁 Структура проекта

```
truespace/
├── app/
│   ├── page.tsx              # Главная страница
│   ├── login/                # Страница входа
│   ├── register/             # Страница регистрации
│   ├── courses/              # Курсы
│   ├── profile/              # Профиль
│   └── admin/                # Админ-панель
│
├── components/               # UI компоненты
├── lib/                     # Утилиты
├── models/                  # MongoDB модели
└── public/                  # Статические файлы
```

## 🔥 Основные URL

- **Главная**: http://localhost:3000
- **Вход**: http://localhost:3000/login
- **Регистрация**: http://localhost:3000/register
- **Курсы**: http://localhost:3000/courses
- **Профиль**: http://localhost:3000/profile
- **Админ**: http://localhost:3000/admin

## 🛠 Полезные команды

```bash
# Запуск в dev режиме
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start

# Проверка линтинга
npm run lint
```

## 🐛 Частые проблемы

### Ошибка подключения к MongoDB

```
MongoServerError: Authentication failed
```

**Решение**: Проверьте правильность `MONGODB_URI` в `.env`

### Порт 3000 занят

```
Error: listen EADDRINUSE: address already in use :::3000
```

**Решение**: 
```bash
# Остановите процесс на порту 3000
lsof -ti:3000 | xargs kill -9

# Или используйте другой порт
PORT=3001 npm run dev
```

### Не работают изображения

**Решение**: Добавьте домены в `next.config.js`:

```javascript
images: {
  domains: ['your-image-domain.com'],
}
```

## 📚 Дополнительная документация

- **Полная документация**: [README.md](README.md)
- **Архитектура**: [ARCHITECTURE.md](ARCHITECTURE.md)
- **Деплой на Render**: [DEPLOYMENT.md](DEPLOYMENT.md)
- **Contributing**: [CONTRIBUTING.md](CONTRIBUTING.md)

## 🎨 Дизайн

Платформа использует темную цветовую схему:

- **Фон**: `#0a0a0a`
- **Карточки**: `#141414`
- **Акцент**: `#ffffff`
- **Вторичный**: `#a0a0a0`

Все компоненты responsive и адаптируются под любые устройства.

## 🚀 Следующие шаги

1. **Настройте AWS S3** для хранения видео
2. **Настройте Email** для восстановления пароля
3. **Добавьте контент** (курсы и уроки)
4. **Задеплойте на Render** (см. DEPLOYMENT.md)

## 🆘 Нужна помощь?

- 📧 Email: support@truespace.com
- 🐛 Issues: [GitHub Issues](https://github.com/yourusername/truespace/issues)
- 📖 Docs: [Полная документация](README.md)

---

**Готово!** Теперь вы можете начать разработку 🎉

Счастливого кодинга! 💻

