# ⚡ Быстрая шпаргалка по деплою

## 🎯 В двух словах

```
Ваш код → GitHub → Render → Работает в интернете!
                      ↓
                  MongoDB (хранит данные)
```

---

## 🔢 Три простых шага

### 1. GitHub - Хранилище кода (5 мин)
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/USERNAME/truespace.git
git push -u origin main
```
✅ **Результат:** Код сохранен на GitHub

### 2. MongoDB - База данных (10 мин)
1. https://mongodb.com/cloud/atlas → Sign Up
2. Create FREE cluster
3. Create user + password
4. Network Access → 0.0.0.0/0
5. Copy connection string

✅ **Результат:** У вас есть MongoDB URI

### 3. Render - Хостинг (15 мин)
1. https://render.com → Sign Up (через GitHub)
2. New + → Web Service
3. Select repository: `truespace`
4. Add Environment Variables:
   ```
   MONGODB_URI = ваш_mongodb_uri
   JWT_SECRET = любая_длинная_строка_32_символа
   NEXTAUTH_SECRET = другая_длинная_строка_32_символа
   NEXTAUTH_URL = https://truespace.onrender.com
   ```
5. Create Web Service

✅ **Результат:** Сайт работает!

---

## 📊 Что где хранится

```
┌─────────────┐
│   GITHUB    │  ← Ваш код (HTML, CSS, JS)
└─────────────┘

┌─────────────┐
│   RENDER    │  ← Запущенный сайт (доступен 24/7)
└─────────────┘

┌─────────────┐
│  MONGODB    │  ← Данные (пользователи, курсы)
└─────────────┘
```

---

## 🔄 Рабочий процесс

### Локальная разработка:
```bash
npm run dev  # http://localhost:3000
```

### Деплой изменений:
```bash
# 1. Изменили код
# 2. Сохранили
git add .
git commit -m "Добавил курс"
git push

# 3. Подождите 2-3 минуты
# 4. Сайт обновлен! https://truespace.onrender.com
```

---

## 💰 Стоимость (бесплатно!)

- GitHub: $0
- Render: $0 (до 750 часов/месяц)
- MongoDB: $0 (до 512MB)

**Этого хватит на 1000+ пользователей!**

---

## 🆘 Если что-то не работает

| Проблема | Решение |
|----------|---------|
| Сайт не открывается | Проверьте Render Dashboard → Logs |
| Не сохраняются данные | Проверьте MongoDB URI в Render |
| Код не обновляется | `git push` + подождите 2-3 мин |

---

## 📖 Детальные инструкции

→ **[DEPLOY_EXPLAINED.md](DEPLOY_EXPLAINED.md)** - Полное объяснение  
→ **[DEPLOYMENT.md](DEPLOYMENT.md)** - Пошаговый гайд

---

**Всё! Теперь вы знаете как это работает!** 🎉

