# Contributing to TrueSpace

Спасибо за интерес к улучшению TrueSpace! Мы приветствуем вклад от сообщества.

## 🚀 Как внести вклад

### 1. Fork репозиторий

Нажмите кнопку "Fork" в правом верхнем углу страницы GitHub.

### 2. Клонируйте свой fork

```bash
git clone https://github.com/your-username/truespace.git
cd truespace
```

### 3. Создайте ветку для вашей функции

```bash
git checkout -b feature/amazing-feature
```

Используйте префиксы:
- `feature/` - для новых функций
- `fix/` - для исправлений
- `docs/` - для документации
- `refactor/` - для рефакторинга
- `test/` - для тестов

### 4. Установите зависимости

```bash
npm install
```

### 5. Настройте окружение

Создайте `.env` файл на основе `.env.local.example`:

```bash
cp .env.local.example .env
```

### 6. Внесите изменения

Следуйте стандартам кода проекта:

#### TypeScript Style Guide
- Используйте TypeScript для всех новых файлов
- Определяйте типы и интерфейсы
- Избегайте `any` типа
- Используйте строгую типизацию

#### React/Next.js Best Practices
- Используйте функциональные компоненты
- Используйте hooks вместо классов
- Следуйте правилам hooks
- Используйте Server Components где возможно

#### CSS/Styling
- Используйте Tailwind CSS утилиты
- Следуйте существующей цветовой палитре
- Используйте responsive классы (sm:, md:, lg:)
- Избегайте inline styles

#### API Routes
- Всегда проверяйте аутентификацию
- Используйте правильные HTTP статус-коды
- Возвращайте структурированные ответы
- Обрабатывайте ошибки gracefully

### 7. Запустите проект локально

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

### 8. Проверьте lint

```bash
npm run lint
```

### 9. Закоммитьте изменения

```bash
git add .
git commit -m "feat: add amazing feature"
```

Используйте conventional commits:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, не влияющее на код
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

### 10. Отправьте изменения

```bash
git push origin feature/amazing-feature
```

### 11. Создайте Pull Request

1. Перейдите на GitHub
2. Нажмите "New Pull Request"
3. Выберите вашу ветку
4. Заполните описание PR:
   - Что изменено
   - Почему изменено
   - Как протестировать
5. Нажмите "Create Pull Request"

## 📝 Pull Request Guidelines

### Описание PR должно включать:

```markdown
## Описание
Краткое описание изменений

## Тип изменения
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Как протестировать
1. Шаг 1
2. Шаг 2
3. ...

## Screenshots (если применимо)
![screenshot](url)

## Checklist
- [ ] Код следует style guide
- [ ] Добавлены комментарии для сложной логики
- [ ] Документация обновлена
- [ ] Нет console.log в коде
- [ ] Lint проходит без ошибок
```

## 🐛 Сообщения об ошибках

При создании Issue включите:

1. **Описание проблемы**
   - Что ожидалось
   - Что произошло на самом деле

2. **Шаги для воспроизведения**
   ```
   1. Перейти на '...'
   2. Нажать на '...'
   3. Увидеть ошибку
   ```

3. **Окружение**
   - OS: [например, macOS 12.0]
   - Browser: [например, Chrome 96]
   - Node version: [например, 18.0.0]

4. **Screenshots**
   Если применимо, добавьте screenshots

5. **Логи**
   ```
   Вставьте логи ошибок здесь
   ```

## 💡 Предложения по улучшению

При предложении новой функции включите:

1. **Проблема**
   Какую проблему решает эта функция?

2. **Предлагаемое решение**
   Как вы предлагаете решить эту проблему?

3. **Альтернативы**
   Какие альтернативные решения вы рассмотрели?

4. **Дополнительный контекст**
   Mockups, примеры из других приложений, etc.

## 🎨 Дизайн Guidelines

### Цветовая палитра

```css
/* Dark Theme (Primary) */
--dark-bg: #0a0a0a;
--dark-card: #141414;
--dark-hover: #1e1e1e;
--dark-border: #2a2a2a;

/* Accent Colors */
--accent-primary: #ffffff;
--accent-secondary: #a0a0a0;
--accent-hover: #e0e0e0;
```

### Типографика

- Заголовки: Font weight 700 (bold)
- Основной текст: Font weight 400 (regular)
- Вторичный текст: `text-accent-secondary`

### Spacing

Используйте Tailwind spacing scale:
- `p-4`, `p-6`, `p-8` для padding
- `space-y-4`, `space-y-6` для вертикальных отступов
- `gap-4`, `gap-6`, `gap-8` для grid/flex gaps

### Анимации

- Используйте Framer Motion для анимаций
- Transitions должны быть плавными (0.3s - 0.6s)
- Используйте easing: `ease-out` или `cubic-bezier`

## 🧪 Testing (будущее)

Мы планируем добавить тесты. Если вы хотите помочь:

```bash
# Unit tests (Jest)
npm test

# E2E tests (Playwright)
npm run test:e2e
```

## 📦 Структура проекта

```
truespace/
├── app/              # Next.js App Router
├── components/       # React компоненты
├── lib/             # Утилиты и helpers
├── models/          # MongoDB модели
└── public/          # Статические файлы
```

## 🔍 Code Review Process

1. Maintainer проверит ваш PR
2. Могут быть запрошены изменения
3. После одобрения PR будет смержен
4. Изменения будут автоматически задеплоены

## 📜 Лицензия

Внося вклад, вы соглашаетесь, что ваш вклад будет лицензирован под MIT License.

## ❓ Вопросы?

Если у вас есть вопросы, создайте Issue или напишите на support@truespace.com

## 🙏 Благодарности

Спасибо всем контрибьюторам за вклад в проект!

---

Сделано с ❤️ для образования

