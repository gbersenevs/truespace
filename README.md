# TrueSpace - Educational Video Platform

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy)

Современная образовательная веб-платформа для размещения видео-уроков с минималистичным дизайном в темно-белой цветовой гамме.

## 🎯 Основные функции

- ✅ **Аутентификация пользователей** - регистрация, вход, восстановление пароля
- 🎟️ **Система промокодов** - доступ к контенту по специальным кодам
- 👤 **Личный профиль** - управление аккаунтом и избранным
- ❤️ **Избранное** - сохранение интересных курсов и уроков
- 🔍 **Поиск и фильтры** - удобный поиск по категориям и уровням
- 🎨 **Современный дизайн** - минималистичный темный интерфейс с анимациями
- 📱 **Адаптивность** - работает на всех устройствах
- 🛡️ **Админ-панель** - управление курсами, уроками и промокодами

## 🛠 Технический стек

### Frontend
- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - типизированный JavaScript
- **Tailwind CSS** - utility-first CSS framework
- **Framer Motion** - библиотека анимаций
- **React Icons** - коллекция иконок

### Backend
- **Next.js API Routes** - серверные API endpoints
- **MongoDB** - NoSQL база данных
- **Mongoose** - ODM для MongoDB
- **JWT** - JSON Web Tokens для аутентификации
- **bcryptjs** - хеширование паролей

### Хранение и деплой
- **YouTube** - хостинг видео (бесплатно!)
- **Render** - хостинг приложения
- **MongoDB Atlas** - облачная база данных
- **Unsplash/Pexels** - бесплатные изображения для курсов

## 📋 Требования

- Node.js >= 18.0.0
- npm или yarn
- MongoDB Atlas account
- YouTube account (для загрузки видео)
- Render account (для деплоя)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/truespace.git
cd truespace
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/truespace?retryWrites=true&w=majority

# Authentication
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
NEXTAUTH_SECRET=your-nextauth-secret-key-minimum-32-characters
NEXTAUTH_URL=http://localhost:3000

# Email (для восстановления пароля - опционально)
EMAIL_SERVER=smtp://username:password@smtp.gmail.com:587
EMAIL_FROM=noreply@truespace.com

# Admin (опционально)
ADMIN_EMAIL=admin@truespace.com
ADMIN_PASSWORD=changeme123
```

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: [http://localhost:3000](http://localhost:3000)

### 5. Сборка для продакшена

```bash
npm run build
npm start
```

## 📊 Схема базы данных

### User (Пользователь)
```typescript
{
  email: string;
  password: string (hashed);
  name: string;
  role: 'user' | 'admin';
  hasAccessToCourses: boolean;
  usedPromoCode?: string;
  favorites: ObjectId[];
  savedLessons: ObjectId[];
  avatar?: string;
  resetPasswordToken?: string;
  resetPasswordExpire?: Date;
}
```

### Course (Курс)
```typescript
{
  title: string;
  description: string;
  thumbnail: string;
  category: string;
  tags: string[];
  instructor: string;
  duration: number;
  level: 'beginner' | 'intermediate' | 'advanced';
  isPublished: boolean;
  lessons: ObjectId[];
  rating: number;
}
```

### Lesson (Урок)
```typescript
{
  title: string;
  description: string;
  videoUrl: string;
  duration: number;
  order: number;
  courseId: ObjectId;
  resources: Array<{
    title: string;
    url: string;
    type: 'pdf' | 'link' | 'file';
  }>;
}
```

### PromoCode (Промокод)
```typescript
{
  code: string;
  isActive: boolean;
  maxUses: number; // -1 для неограниченного
  currentUses: number;
  expiresAt?: Date;
  description?: string;
}
```

## 🎨 Дизайн-система

### Цветовая палитра
```css
--dark-bg: #0a0a0a;        /* Основной фон */
--dark-card: #141414;       /* Карточки */
--dark-hover: #1e1e1e;      /* Hover состояния */
--dark-border: #2a2a2a;     /* Границы */
--accent-primary: #ffffff;  /* Основной акцент */
--accent-secondary: #a0a0a0;/* Вторичный текст */
```

### Анимации
- Плавные переходы между страницами
- Hover эффекты на карточках
- Анимированные кнопки
- Shimmer эффект для загрузки
- Fade-in/Scale анимации для появления элементов

## 🔐 API Endpoints

### Аутентификация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход
- `GET /api/auth/me` - получить данные текущего пользователя

### Курсы
- `GET /api/courses` - получить список курсов (с фильтрами)
- `GET /api/courses/[id]` - получить курс по ID
- `POST /api/courses` - создать курс (только admin)
- `PUT /api/courses/[id]` - обновить курс (только admin)
- `DELETE /api/courses/[id]` - удалить курс (только admin)

### Промокоды
- `POST /api/promo/validate` - активировать промокод
- `GET /api/admin/promo-codes` - получить все промокоды (только admin)
- `POST /api/admin/promo-codes` - создать промокод (только admin)

### Избранное
- `GET /api/favorites` - получить избранные курсы
- `POST /api/favorites` - добавить/удалить из избранного

## 🚀 Деплой на Render

### Автоматический деплой

1. Создайте аккаунт на [Render.com](https://render.com)
2. Нажмите кнопку "Deploy to Render" вверху README
3. Подключите GitHub репозиторий
4. Настройте переменные окружения
5. Нажмите "Create Web Service"

### Ручной деплой

1. Создайте новый Web Service на Render
2. Подключите ваш GitHub репозиторий
3. Настройте параметры:
   - **Name**: truespace
   - **Region**: Oregon (US West)
   - **Branch**: main
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Starter (или выше)

4. Добавьте переменные окружения (Environment Variables):
   - `MONGODB_URI` - строка подключения к MongoDB Atlas
   - `JWT_SECRET` - секретный ключ для JWT
   - `NEXTAUTH_SECRET` - секретный ключ для NextAuth
   - `NEXTAUTH_URL` - URL вашего приложения на Render
   - `AWS_ACCESS_KEY_ID` - AWS ключ доступа
   - `AWS_SECRET_ACCESS_KEY` - AWS секретный ключ
   - `AWS_REGION` - AWS регион (например, us-east-1)
   - `AWS_S3_BUCKET` - имя S3 bucket
   - `EMAIL_SERVER` - SMTP сервер
   - `EMAIL_FROM` - email отправителя

5. Нажмите "Create Web Service"

## 📱 Настройка MongoDB Atlas

1. Создайте аккаунт на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте новый кластер (бесплатный tier доступен)
3. Создайте пользователя базы данных
4. Добавьте IP адрес в whitelist (0.0.0.0/0 для доступа отовсюду)
5. Получите строку подключения и добавьте в `MONGODB_URI`

## 🎥 Настройка YouTube для видео

1. Создайте YouTube канал (если еще нет)
2. Загрузите ваши образовательные видео
3. Установите приватность:
   - **Public** - для открытого доступа
   - **Unlisted** - видео доступно только по ссылке (рекомендуется)
4. Скопируйте URLs видео (например: `https://www.youtube.com/watch?v=VIDEO_ID`)
5. Используйте эти URLs при создании уроков на платформе

**Преимущества YouTube:**
- ✅ Бесплатный хостинг видео
- ✅ Автоматическая адаптация качества
- ✅ CDN по всему миру
- ✅ Не нужно настраивать облачное хранилище
- ✅ Автоматические субтитры (опционально)

## 👨‍💼 Создание первого администратора

После первого деплоя, создайте admin пользователя вручную в MongoDB:

```javascript
{
  email: "admin@truespace.com",
  password: "$2a$10$hashedPassword", // используйте bcrypt для хеширования
  name: "Admin",
  role: "admin",
  hasAccessToCourses: true,
  favorites: [],
  savedLessons: []
}
```

Или используйте API для регистрации, затем обновите `role` на `admin` в базе данных.

## 📝 Создание промокода

1. Войдите как администратор
2. Перейдите на `/admin`
3. Выберите "Promo Codes"
4. Нажмите "Create Promo Code"
5. Введите данные промокода:
   - **Code**: уникальный код (например, WELCOME2024)
   - **Max Uses**: максимальное количество использований (-1 для неограниченного)
   - **Expires At**: дата истечения (опционально)
   - **Description**: описание промокода

## 🎓 Добавление курсов

1. **Загрузите видео на YouTube:**
   - Загрузите образовательные видео на ваш YouTube канал
   - Установите приватность "Unlisted" (доступ только по ссылке)
   - Скопируйте URL каждого видео

2. **Найдите изображение для курса:**
   - Используйте бесплатные фото с [Unsplash](https://unsplash.com/)
   - Или превью с YouTube: `https://img.youtube.com/vi/VIDEO_ID/maxresdefault.jpg`

3. **Создайте курс на платформе:**
   - Войдите как администратор
   - Перейдите на `/admin`
   - Выберите "Courses"
   - Нажмите "Create New Course"
   - Заполните информацию о курсе
   - В поле `thumbnail` вставьте URL изображения
   
4. **Добавьте уроки:**
   - В поле `videoUrl` вставьте YouTube URL (например: `https://www.youtube.com/watch?v=pQN-pnXPaVg`)
   - Укажите длительность в секундах
   - Установите порядок урока

**Пример YouTube URLs:**
```
https://www.youtube.com/watch?v=pQN-pnXPaVg
https://youtu.be/pQN-pnXPaVg
```

## 🐛 Troubleshooting

### Проблема: Не удается подключиться к MongoDB
- Проверьте правильность строки подключения
- Убедитесь, что IP адрес добавлен в whitelist
- Проверьте username и password

### Проблема: Видео не воспроизводятся
- Убедитесь, что YouTube видео установлено как Public или Unlisted (НЕ Private)
- Проверьте правильность YouTube URL
- Убедитесь, что `react-player` установлен: `npm install react-player`

### Проблема: Email не отправляются
- Проверьте SMTP настройки
- Для Gmail включите "Less secure app access"
- Используйте App Password если включена 2FA

## 📄 Лицензия

MIT License - вы можете свободно использовать этот проект.

## 👥 Поддержка

Если у вас возникли вопросы или проблемы:
- Создайте Issue на GitHub
- Напишите на email: support@truespace.com

## 🙏 Благодарности

- Next.js team за отличный фреймворк
- Framer Motion за потрясающие анимации
- MongoDB за надежную базу данных
- Render за простой деплой

---

Сделано с ❤️ для образования

